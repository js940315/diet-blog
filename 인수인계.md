# 매일 10편 스탠바이 시스템 — 인수인계

새 컴퓨터 · 새 Claude 계정 · 새 GitHub 계정에서 **다른 주제**로 같은 걸 다시 만들 때 보는 문서.
다이어트 블로그(`diet-blog` / 브랜드 "건강비버")를 운영하며 실제로 깨진 것들을 근거로 적었다.
주제별 글쓰기 규칙은 여기 없다 — 그건 매번 새로 만드는 것이고, 여기 있는 건 **뼈대와 함정**이다.

---

## 1. 이 시스템이 하는 일

> 매일 새벽 클라우드가 글 10편 + 사진을 만들어 GitHub에 올려두면,
> 아침에 사람이 받아서 네이버에 복붙 발행한다.

핵심은 **자동 발행이 아니다.** 발행은 사람이 한다. 자동화하는 건 "발행 직전 상태까지".
네이버는 API 발행이 사실상 막혀 있고, 자동 발행은 저품질 판정 위험도 크다.
그래서 목표는 **"열어서 Ctrl+A, Ctrl+C, Ctrl+V 하면 끝"인 완성본**을 매일 아침 대기시키는 것.

이 구조를 지키면 얻는 게 있다:
- 사람이 최종 검수 게이트라서, 이상한 글이 그대로 나가지 않는다
- 클라우드가 죽어도 사람이 이어받을 수 있다
- 발행 시점을 사람이 조절할 수 있다 (홈판 노출 시간대 실험 등)

---

## 2. 왜 GitHub를 쓰는가

클라우드 세션은 **매번 새로 태어나고 끝나면 사라진다.** 로컬 디스크가 없다.
그래서 결과물을 남기려면 어딘가에 push 해야 하고, 그게 GitHub다.

GitHub는 세 가지 역할을 동시에 한다:

| 역할 | 내용 |
|---|---|
| **배달** | 클라우드가 만든 글·사진을 사람 컴퓨터로 옮기는 통로 (GitHub Desktop으로 pull) |
| **상태 저장** | 인물 쿨다운·기사 중복·CTA 로테이션 기록(`state/`)을 다음 실행이 이어받는다 |
| **로그** | 커밋 기록이 곧 "어디까지 하고 죽었는지"의 증거다 |

⚠️ `state/`와 `output/`을 `.gitignore` 하면 안 된다. 무시하는 순간 시스템이 반쪽이 된다.
`state/store.db`가 안 올라가면 실행마다 초기화돼서 같은 인물이 매일 반복해서 뽑힌다.

---

## 3. 레포 구조

```
<블로그-repo>/
├─ .claude/commands/daily.md   ← 하루 절차서. 루틴이 매번 읽는다. 규칙의 원본.
├─ README.md                   ← 시스템 설명. 루틴이 매번 읽는다.
├─ config.py                   ← 숫자·금지어·인물풀. 튜닝은 전부 여기서.
├─ main.py                     ← 4단계 파이프라인 진입점
├─ crawler.py                  ← 소스 수집
├─ prompts.py                  ← 단계별 "지시서"를 생성 (에이전트가 읽을 문서)
├─ titles.py                   ← 제목 점수 함수
├─ draft_to_body.py            ← 평문 초안 → 발행 규격 본문
├─ validator.py                ← 규격 검사. 여기 통과 못 하면 마감 안 됨.
├─ images.py / photo_*.py      ← 사진 선택·소싱
├─ assets/photos/              ← 사진 라이브러리 + index.json
├─ state/                      ← store.db(이력), work/(작업파일), 사용기록
└─ output/<MMDD>/<슬롯>/       ← ★ 완성본. 사람이 여는 곳.
      ├─ 0번 본문.txt
      ├─ 1~4번 사진.jpg
      └─ 예비제목.txt          ← 확정 제목이 맘에 안 들 때 갈아끼울 후보
```

### output 규칙 — 이게 계약이다

> **폴더가 있다 = 발행 가능하다.**

- 완성되기 전에는 폴더를 만들지 않는다. 아침에 빈 폴더를 보면 "만들다 죽었나?" 하고 헷갈린다.
- 발행한 슬롯은 사람이 폴더를 지운다. **지워져 있다 = 발행 끝.**
- 날짜 폴더는 `MMDD`(0808). 전 블로그 공통 규격.

---

## 4. 파이프라인 4단계

```
crawl  →  (에이전트: 팩트시트 + 제목후보 10개)
title  →  (점수 함수가 제목 확정. 에이전트가 고르지 않는다)
본문   →  (에이전트: draft.txt 평문) → draft_to_body.py
finish →  validator 통과 + 사진 부착 → output/ 에 완성본
```

### 단계별 원칙

**crawl** — 소스를 모으고 슬롯마다 "지시서"(`1_팩트시트_지시서.md`)를 만든다.
에이전트는 그 지시서만 읽고 `factsheet.json`을 쓴다. 소스에 없는 건 빈 값으로 둔다.

**title** — 에이전트는 **후보 10개만** 낸다. 고르는 건 `titles.py`의 점수 함수다.
이게 중요하다. 에이전트에게 고르게 하면 매번 무난한 걸 고른다.
후보는 서로 다른 각도로 최대한 벌려서 내게 하고, 선택은 기계에 맡긴다.

**본문** — 에이전트는 **평문 `draft.txt`만** 쓴다.
특수문자(점자 빈칸 등)를 손으로 넣게 하면 반드시 빠진다. 변환기가 붙인다.

**finish** — `validator.py`가 규격을 검사한다. 위반이 있으면 마감을 거부하고 목록을 준다.
에이전트는 `draft.txt`를 고쳐 다시 돌린다. 위반 0이 될 때까지.

---

## 5. ★ 가장 중요한 원칙 — 지시서만으로는 샌다

**규칙을 프롬프트에만 적으면 반드시 새어 나온다.** 실측으로 증명된 것들:

| 규칙 | 프롬프트에만 있었을 때 결과 |
|---|---|
| "언론사명 쓰지 마라" | 10편에 49번 등장 |
| "제목 음식과 사진을 맞춰라" | 10편 중 1편만 맞음 |
| "직업어에 여를 붙여라" | 제목 4건·본문 25건 누락 |

그래서 규칙은 **세 군데에 동시에** 둔다:

1. **`prompts.py`** — 에이전트가 읽는 지시서 (무엇을 하라)
2. **`validator.py`** — 기계가 검사 (안 했으면 마감 거부)
3. **`config.py`** — 목록·숫자 (튜닝 지점)

> **검사할 수 없는 규칙은 지켜지지 않는 규칙이다.**
> 새 규칙을 넣을 때마다 "이걸 코드로 어떻게 잡지?"를 먼저 생각해라.

### 검증기를 쓸 때 조심할 것

단순 문자열 검사는 오탐을 만든다. 실제 사례:
- `배우` 금지 → `배우다`(배우고, 배우기), `배우자`(남편/아내)까지 잡힘
- 해결: 뒤에 명사 조사가 붙을 때만 직업어로 판정. **자체 테스트 케이스를 같이 넣어라.**

`validator.py`와 `images.py` 하단에 `if __name__ == "__main__":` 자체 테스트가 있다.
새 규칙마다 회귀 케이스를 추가해라. 나중에 규칙끼리 충돌할 때 이게 유일한 방어선이다.

---

## 6. 클라우드 루틴 세팅

Claude의 **클라우드 루틴(routine)** 기능을 쓴다. cron으로 클라우드 세션이 뜨고,
레포를 클론해서 작업하고, commit·push하고 끝난다.

### 세팅 순서

1. **GitHub 레포 생성** (private 가능)
2. ★ **Claude GitHub App을 그 레포에 "설치"한다**
   → https://github.com/apps/claude/installations/new
   OAuth "인증(Authorized)"만으론 **push가 403**이다. 레포마다 별도 "설치(Installed)"가 필요하다.
   `github.com/settings/installations`에서 Installed 목록에 그 repo가 있는지 확인.
3. **루틴 생성** — claude.ai/code/routines 또는 API
   - `cron_expression`은 **UTC**다. KST 02:00 = UTC 17:00 (전날). 최소 간격 1시간.
   - `sources`에 레포 URL
   - `notifications.push = true` ← **빼먹지 마라.** 없으면 실패해도 조용히 묻힌다.
4. **루틴을 쪼갠다** — 한 세션이 10편을 다 쓰면 무겁다. A(1~5) / B(6~10) 두 개로.
5. **재시도를 넣는다** — `0 17,19 * * *` 처럼 2회. 한 번 죽어도 다음이 이어받는다.

### 루틴 프롬프트에 반드시 넣을 것

```
0단계 — 하트비트 (다른 어떤 일보다 먼저)
  git pull → state/routine_heartbeat.md 에 "루틴명 | UTC시각 | 시작" 한 줄 추가
  → 그 파일만 커밋하고 즉시 push
  → push 실패하면(403 등) 여기서 즉시 중단하고 알림. 글을 쓰지 마라. 시간 낭비다.

증분 푸시
  슬롯 하나가 위반 0으로 끝날 때마다 즉시 commit + push.
  절대 모아서 한 번에 push 하지 마라. 세션이 죽으면 전부 증발한다.
  push 전 항상 git pull --rebase.

이미 채워진 슬롯은 절대 재생성하지 마라
  output/<MMDD>/<슬롯>/ 에 본문 파일이 있으면 완성된 것이다. 손대지 마라.
  사람이 밤에 미리 채워뒀을 수 있다.

날짜는 KST 기준
  이 머신은 UTC라 시스템 날짜가 전날이다. 날짜를 직접 계산하지 말고 코드가 정하게 둬라.
```

### 아침에 상태 판정하는 법

`state/routine_heartbeat.md`만 보면 된다.

- **줄이 있는데 글이 없다** → 발화·push는 됐고 **작업 중 죽은 것**
- **줄이 없다** → 발화 자체가 안 된 것 (루틴 등록·활성 상태 확인)

---

## 7. 하루 흐름

```
02:00 KST  루틴 A 발화 → 하트비트 push → 슬롯 1~5 (하나씩 즉시 push)
03:30 KST  루틴 B 발화 → 슬롯 6~10 (+ 1~5 중 빈 것 있으면 이어받음)
04:00 KST  루틴 A 재시도 (빈 슬롯만)
05:30 KST  루틴 B 재시도 (빈 슬롯만)
아침       사람이 GitHub Desktop으로 pull
           → output/<오늘>/1~10/ 열어서 본문 복붙, 사진 첨부, 발행
           → 발행한 슬롯 폴더 삭제
           → 삭제분은 "발행 완료분 정리" 같은 별도 커밋으로 기록
```

---

## 8. 실제로 겪은 함정 — 새 블로그에서도 그대로 밟는다

### (1) `git add -A`가 삭제를 쓸어담는다 ★
사람이 발행 후 슬롯 폴더를 지운 상태에서 다른 작업을 `git add -A`로 커밋하면,
그 삭제까지 같이 기록된다. 나중에 보면 데이터 유실로 보여서 복구 소동이 난다.

**대책:** 발행분 삭제는 **항상 별도 커밋**으로 분리한다. 작업 커밋에 섞지 마라.
스테이징은 경로를 명시해서 한다 (`git add output/0808 config.py`).

### (2) 몰아서 push하면 다 날아간다 ★
세션이 중간에 죽으면 결과물이 전부 증발한다. 이틀 연속 당했다.
**대책:** 슬롯 하나 끝날 때마다 즉시 push. 죽어도 완성분은 남고, 커밋 기록이 사망 지점을 알려준다.

### (3) push 권한 403
`Resource not accessible by integration`. 인증만 하고 레포에 앱 **설치**를 안 한 것.
**대책:** 6장 2번. 하트비트로 조기 발견.

### (4) UTC/KST 날짜 밀림
UTC 머신에서 전날 21:00에 도는 루틴은 시스템 날짜가 전날이다.
실제로 08-05 실행분이 `output/0804`에 들어가 "오늘자 없음" 사고가 났다.
**대책:** 날짜는 코드가 KST로 정하게 하고, 루틴 프롬프트가 직접 계산하지 못하게 막는다.

### (5) 사진이 글과 따로 논다
제목이 "호박식혜 해프닝"인데 아보카도 사진이 나갔다. 10편 중 1편만 맞았다.
원인: 미리 정의한 버킷에 문자열이 걸릴 때만 반영하고, 안 걸리면 기본값을 넣었다.
**대책:** 제목·본문에서 실제 소재를 뽑아 **그 소재로 사진을 고른다.** 없으면 그 자리에서 소싱.
**1번 사진이 홈판 썸네일**이라 제목과 어긋나면 그냥 낚시로 읽힌다.

### (6) 사진 사용기록이 재실행 때 안 지워진다
같은 슬롯을 다시 마감하면 직전 선택을 회수하지 않고 새 선택만 누적한다.
그래서 **재실행할수록 그날 사진 풀이 줄어든다.** 사진을 채우려고 재실행했다가 오히려 깎였다.
**대책:** 전량 재생성이면 그날 사용기록을 비우고 시작. 부분이면 `report.json`의 실사용분으로 재구성.

### (7) 검색·수집의 동명이인
"양파"(가수)로 검색했더니 69건 전부 채소·타인이었다. 일반명사 예명은 함정이다.
다른 사람의 수치를 주인공 것으로 옮겨 적는 사고가 실제로 났다.
**대책:** 지시서에 "이름이 실제로 들어간 소스만 근거로 써라"를 명시하고, 인물 풀 주석에 기록.

### (8) 소스가 얇으면 얇은 대로
검색 API가 막히면 헤드라인만 들어온다. 그때 분량을 억지로 채우면 창작이 된다.
**대책:** **"짧게 내는 게 지어내는 것보다 낫다"**를 지시서 최상위에 둔다.
하루 10편은 **상한이지 할당량이 아니다.** 근거가 없으면 건너뛰고 보고하게 한다.

### (9) 마크다운이 그대로 보인다
네이버 에디터는 마크다운을 해석하지 않는다. `**강조**`가 화면에 그대로 나온다.
**대책:** 지시서에서 금지 + 검증기에서 검사.

### (10) 대량 이미지 렌더는 로컬에서 하지 마라
블루스크린을 유발한 적 있다. 클라우드 루틴에 맡긴다.

---

## 9. 세션 부담 — 이게 성패를 가른다 ★

같은 계정의 블로그 4개를 비교했을 때 **성공/실패를 가른 건 이것 하나였다.**

| 블로그 | 구조 | 결과 |
|---|---|---|
| 자동차·경제 | LLM은 **JSON 스펙만** 쓰고 파이썬 빌더가 렌더링 | 매일 정상 |
| 뷰티·패션 | 이미지 생략 + 하루 3회 재시도 | 매일 정상 |
| **다이어트** | **한 세션이 5편을 손으로 쓰고 위반 0까지 반복 수정 + 사진 처리, 시도 1회** | **5일 연속 실패** |

다이어트는 슬롯당 6~10만 토큰이 들었다. 단일 세션이 5편을 연속으로 해내지 못한다.

**새 블로그를 만들 때 처음부터 정할 것:**
- LLM이 하는 일을 **최소화**한다. 사실 추출과 문장 생성만 시키고, 조립·검사·렌더링은 코드가 한다.
- 가능하면 **"LLM은 JSON을 쓰고 빌더가 만든다"** 구조로 간다.
- 그게 어려우면 **재시도 횟수를 늘린다.**
- 세션이 읽어야 할 문서를 줄인다. README가 11KB면 그것도 부담이다.

---

## 10. 새 주제로 폼 만드는 순서

벤치마킹 블로그를 주고 새로 만들 때.

1. **벤치마킹 대상 분석** — 제목 20~30개를 모아서 공통 구조를 뽑는다.
   (건강비버는 "이슈 선행 + 모순(A인데 B) + 블라인드"가 공식이었다)
2. **제목 점수 함수부터 만든다** (`titles.py`)
   - 벤치마킹 제목들을 넣고 실제로 높은 점수가 나오는지 확인
   - 자체 테스트에 기대값을 박아둔다
3. **본문 규격을 정한다** — 줄 길이, 챕터 수, 분량, 도입부 구조, CTA
   - **플랫폼 실측이 먼저다.** 네이버는 점자 빈칸도 글자로 센다는 걸 몰라 분량이 계속 틀렸다.
4. **금지선을 정한다** — 주제별로 다르다.
   건강 주제는 규제가 세다: 효과 단정 금지, 처방의약품 금지, 개인 건강조언 금지, 실존 인물 신체 평가 금지.
5. **규격을 validator로 옮긴다** ← 5장. 여기서 대충 하면 나중에 다 새어 나온다.
6. **사진 전략을 정한다** — 라이선스 확인 필수(Commons / Pexels / Unsplash / Pixabay).
   인물 사진은 초상권 때문에 안 쓰는 게 안전하다.
7. **파일럿 1편**을 손으로 끝까지 돌려본다. 여기서 규격 구멍이 다 나온다.
8. **10편으로 늘린다** — 같은 날 소재·인물·사진이 겹치지 않는지 확인.
9. **루틴 등록** (6장)
10. **첫 주는 매일 아침 결과를 본다.** 자동화는 첫 주에 반드시 깨진다.

---

## 11. 인수인계 체크리스트

새 환경에서 처음부터 할 일:

- [ ] GitHub 계정 생성, 레포 생성
- [ ] **Claude GitHub App을 레포에 설치** (인증만으론 403)
- [ ] 로컬에 GitHub Desktop 설치, 클론
- [ ] Python + 의존성 (`requirements.txt`)
- [ ] `.env` 준비 — 검색 API 키, 스톡 사진 API 키 (없어도 Commons로 동작하게 설계할 것)
- [ ] `.gitignore`에서 `state/`·`output/` 제외 안 했는지 확인
- [ ] 파일럿 1편 로컬에서 완주
- [ ] 클라우드 루틴 A/B 등록 (UTC 변환 확인, 알림 켜기, 재시도 2회)
- [ ] 루틴 프롬프트에 하트비트·증분푸시·재생성금지·KST 4종 포함
- [ ] 루틴 수동 실행 1회 → **하트비트 커밋이 origin에 올라오는지 눈으로 확인**
- [ ] 첫 새벽 결과 확인

---

## 12. 한 줄 요약

> 클라우드가 매일 새벽 완성본을 GitHub에 쌓아두고, 사람이 아침에 받아 발행한다.
> **규칙은 검증기에 넣어야 지켜지고, 결과물은 슬롯마다 push해야 남는다.**
> 세션에 일을 적게 시킬수록 안 죽는다.
