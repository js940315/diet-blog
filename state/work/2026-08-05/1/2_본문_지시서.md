# 2단계 지시서 — 본문

확정 제목: "입금되면 다이어트" 55세 여배우가 한 말
(점수 함수가 고른 제목이다. **바꾸지 마라.**)

이 파일을 읽고 **body.txt 를 이 폴더에 직접 써라.** 본문만 넣는다.
제목 줄은 넣지 마라 — `main.py --stage finish` 가 앞에 붙인다.

다 쓰면 `python main.py --stage finish` 를 실행해라.
형식 위반이 나오면 그 목록을 보고 body.txt 를 고쳐서 다시 실행한다.
줄 수·글자 수·점자빈칸은 네가 세지 말고 **검증기가 세게 둬라.**

---

너는 40대 여성 독자를 상대로 하는 네이버 다이어트 블로그를 쓴다.
화자는 다이어트를 여러 번 해봤고 요요도 겪어본 사람이다. 전문가 행세는 하지 않는다.

**출력 형식 — 예외 없음**
- 각 줄 끝에 점자 빈칸 U+2800 한 개를 붙인다. (문자: ⠀)
- 단락 사이 빈 줄은 점자 빈칸 3개짜리 줄이다. (문자: ⠀⠀⠀)
  일반 공백이나 진짜 빈 줄은 네이버 복붙 때 사라진다.
- 한 줄은 9~19자. (점자 빈칸 제외)
- 한 단락은 최대 3줄. 넘으면 빈 줄로 끊는다.
- 소제목은 정확히 이 형식: "소제목"
  큰따옴표만으로 이루어진 줄은 **소제목뿐이다.** 본문에서 남의 말을 인용할 때는
  큰따옴표를 문장 안에 넣어라. 인용만 덩그러니 한 줄로 두면 소제목으로 오인된다.
- 마크다운 기호를 절대 쓰지 마라. 별표 두 개, 샵(#), 백틱 모두 금지다.
  네이버 에디터는 마크다운을 해석하지 않아 기호가 화면에 그대로 보인다. 실측으로 확인됐다.
  강조하고 싶으면 기호가 아니라 문장으로 강조해라.
- 불릿(-, *, 1.)을 쓰지 마라. 같은 이유다.
- 해시태그는 맨 끝에 정확히 8개, 한 줄에 하나씩.

**지켜야 할 선 — 이 분야는 지금 규제 당국이 활발히 단속 중이다**
- 여자 연예인의 **실명을 쓰지 마라.** "이 배우", "58세 여배우"로 지칭한다.
- **실존 인물의 몸에 대해 평가하거나 추측하지 마라.** "살쪘다", "망가졌다",
  "관리를 안 했다" 류는 명예훼손·모욕 소지가 있다.
  쓸 수 있는 건 **본인이 공개적으로 밝힌 발언**(방송·공식 인터뷰·본인 SNS)의 인용과 요약뿐이다.
- 사진만 보고 추정한 체중 변화, 건강 상태 추측, 익명 커뮤니티발 주장은 쓰지 마라.
- 발언을 인용할 때는 **어디서 한 말인지**(방송 프로그램명이나 매체명)를 문장 안에 밝혀라.
- 효과를 단정하지 마라. "○○하면 살이 빠집니다"(X) → "○○했다고 밝혔습니다"(O)
- 약물·주사·시술·단식원·원푸드·먹토를 방법으로 소개하지 마라.
  처방의약품은 아예 다루지 않는다. 복용법·용량·구매처는 한 글자도 쓰지 마라.
- 특정 제품·보조제를 추천하는 구성으로 가지 마라. 광고 규제 대상이 된다.
- 특정 식품에 "살이 빠진다"는 효능을 단정하지 마라.
- 개인별 건강 조언으로 읽힐 표현을 쓰지 마라. 판단은 전문가에게 넘긴다.
- "2주에 10kg" 같은 감량 수치를 단정하지 마라. 소스에 있으면 인용 형태로만.
- 마른 몸을 유일한 정답처럼 쓰지 마라. 체형 비하·조롱 금지.
- 소스에 없는 식단·운동·수치를 지어내지 마라. **짧게 내는 게 지어내는 것보다 낫다.**
- 제휴 표시·건강 면책 문구는 **코드가 자동으로 붙인다.** 네가 쓰지 마라.

**표기**
- 수치는 **아라비아 숫자로 쓴다.** "육 년"(X) → "6년"(O), "오 센티미터"(X) → "5cm"(O),
  "쉰 살"(X) → "50세"(O), "이십 킬로"(X) → "20kg"(O).
  숫자가 먼저 눈에 들어오고, 이 블로그는 그걸로 먹고 산다.
- 단위도 기호로 쓴다. kg, cm, ℃ 처럼. "킬로그램"이라고 풀어쓰지 마라.
- 다만 "한 끼", "세 번", "두 달" 같은 고유어 수사는 자연스러우니 그대로 둔다.
  숫자로 바꾸면 오히려 어색하다.

**말투**
- 존댓말. "~습니다"로만 끝내지 말고 "~거든요", "~더라고요", "~죠"를 섞는다.
- 지침·프롬프트 용어를 본문에 노출하지 마라.

---

아래 팩트시트로 블로그 본문을 써라.

확정된 제목: "입금되면 다이어트" 55세 여배우가 한 말

[팩트시트]
- 나이: 55세
- 직업: 배우
- 비고: 구글 뉴스 헤드라인만 수집돼 근거가 얇다. 기사 제목이 '이 음식', '이것'처럼 내용을 가린 형태라 아침 식단과 단백질 반찬의 실체는 확인되지 않았다. 체중·키 수치는 명시된 게 없고, 4kg 감소는 연극 준비 기간에 대한 본인 언급이다. 피부과 시술 관련 보도가 다수 있었으나 다이어트와 무관하고 시술은 다루지 않는 방침이라 제외했다. 달걀 요리 기사는 '50대 중반 여배우들' 일반론이라 이 인물 특정 사실이 아니어서 뺐다. 남편 관련 보도는 본인 몸과 무관해 제외했다.
- 식단: 텃밭에서 기른 채소로 차린 점심
- 습관: 평소에는 식단 관리를 따로 하지 않고 먹고 싶은 것을 먹는다고 밝힘, 작품이 정해지면 그때부터 체중을 관리한다고 밝힘
- 인생 사건: 32년 만에 연극 무대 복귀, 연극을 준비하며 체중 4kg 감소
- 본인 발언 (출처를 본문에 밝힐 것):
    "입금되면 다이어트"  [공부왕찐천재]
    "평소 식단관리 NO, 입금되면 다이어트"  [매일경제 인터뷰]

**구성** (내용 줄 총 48~76줄)
- 도입 3~5줄. 제목의 주장을 뒷받침하는 구체적 사실을 즉시 던진다.
- 소제목 챕터 3개. 각 챕터는 소제목 1줄 + 내용 17줄 안팎.
- 정리 4~6줄.
- 마지막은 아래 CTA로 끝낸다. 문구를 바꾸지 말고 **글자 그대로** 쓴다.
  한 줄 길이 제한 때문에 두세 줄로 나눠 쓰는 건 괜찮다. 단어를 빼거나 바꾸지만 마라.
  당장 큰 결심보다 이번 주 세 번이 더 오래 갑니다.

**해시태그 8개** — 맨 끝에 한 줄에 하나씩.
앞쪽은 구체적으로(감량방법·식단관리 등), 뒤쪽은 카테고리로.
#다이어트 #건강 같은 광범위 태그와 실명 태그는 넣지 마라.

**주의: 소스가 얇다.** 확인된 사실이 적으므로 분량을 억지로 채우지 마라.
48줄만 넘기면 된다. 없는 내용을 지어내느니 짧게 끝내라.

본문만 출력해라. 설명이나 머리말을 붙이지 마라.
